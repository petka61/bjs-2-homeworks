class AlarmClock {
  constructor() {
    alarmCollection() {
      let arr = [];
    }
    this.timerId = timerId;

    addClock(time, action, id) {
      if (id === undefined) {
        throw new Error("No id");
      }
      
    }
  }
}
